.pio
.vscode/.browse.c_cpp.db*
.vscode/c_cpp_properties.json
.vscode/launch.json
.vscode/ipch

# Local Wi-Fi credentials must never be committed.
include/secrets.h

# Recorded CSI datasets stay out of the repository: they are large, and they
# are experiment data rather than source.
*.jsonl

node_modules/
